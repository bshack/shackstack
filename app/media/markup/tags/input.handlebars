{{#if this.id}}<input id="{{this.id}}"{{#if this.class}} class="{{this.class}}"{{/if}}{{#if this.placeholder}} placeholder="{{this.placeholder}}"{{/if}}{{#if this.type}} type="{{this.type}}"{{else}} type="text"{{/if}}{{#if this.name}} name="{{this.name}}"{{/if}}{{#if this.value}} value="{{this.value}}"{{/if}}{{#if this.required}} required{{/if}}{{#if this.disabled}} disabled{{/if}}{{#if this.checked}} checked{{/if}}{{#if this.labelledby}} aria-labelledby="{{this.labelledby}}"{{/if}}>{{/if}}
