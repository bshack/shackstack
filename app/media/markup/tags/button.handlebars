{{#if this.id}}<button id="{{this.id}}"{{#if this.class}} class="{{this.class}}{{#if this.selected}} selected{{/if}}"{{/if}}{{#if this.accessible-title}} aria-label="{{this.accessible-title}}"{{/if}}{{#if this.disabled}} disabled{{/if}}{{#if this.type}} type="{{this.type}}"{{/if}}{{#if this.role}} role="{{this.role}}"{{/if}}{{#if this.tabindex}} tabindex="{{this.tabindex}}"{{/if}}>{{this.title}}</button>{{/if}}
