{{#if this.id}}<textarea id="{{this.id}}"{{#if this.class}} class="{{this.class}}"{{/if}}{{#if this.placeholder}} placeholder="{{this.placeholder}}"{{/if}}{{#if this.name}} name="{{this.name}}"{{/if}}{{#if this.required}} required{{/if}}{{#if this.disabled}} disabled{{/if}}>{{#if this.value}}{{this.value}}{{/if}}</textarea>{{/if}}
